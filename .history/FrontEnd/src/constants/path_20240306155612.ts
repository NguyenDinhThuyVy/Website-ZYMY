const path = {
  home: '/',
  profile: '/profile',
  login: '/login',
  register: '/register',
  logout: '/logout',
  productDetail: ':id',
  forgetpassword: '/forgetpassword'
} as const

export default path
