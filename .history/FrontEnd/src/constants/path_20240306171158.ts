const path = {
  home: '/',
  profile: '/profile',
  login: '/login',
  register: '/register',
  logout: '/logout',
  productDetail: ':nameId'
  forgetpassword: '/forgetpassword'
} as const

export default path
